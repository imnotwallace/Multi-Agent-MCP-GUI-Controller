# Multi-Agent MCP GUI Controller Dependencies

# Required for performance version
cachetools>=5.0.0  # For TTL caching and performance optimization

# Server dependencies (optional - used by the lightweight MCP server)
fastapi>=0.95.0
uvicorn[standard]>=0.22.0
websockets>=11.0.0

# OS keyring for secure token storage
keyring>=23.13.1

# Built-in modules (no installation needed)
# sqlite3 - Database operations
# tkinter - GUI framework
# threading - Background operations
# logging - Application logging